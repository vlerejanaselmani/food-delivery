# Shija API

Laravel 13 and Sanctum REST API. See [the monorepo README](../README.md) for installation, MySQL setup, API routes, and tests. The React app and its npm dependencies are in `../frontend/`.
